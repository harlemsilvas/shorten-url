# Example .env for local development (DO NOT COMMIT secrets)
# Copy this file to `.env` and fill the values before running `run-local.ps1`

# Either provide a JDBC URL directly:
# JDBC_DATABASE_URL=jdbc:postgresql://hopper.proxy.rlwy.net:18777/railway

# Or provide the public DATABASE URL from Railway (will be parsed by run-local.ps1):
# DATABASE_PUBLIC_URL=postgresql://postgres:YOUR_PASSWORD@hopper.proxy.rlwy.net:18777/railway

# Preferred explicit env vars (recommended):
# JDBC_DATABASE_USERNAME=postgres
# JDBC_DATABASE_PASSWORD=YOUR_PASSWORD

# Optional: application profile to run locally (default: prod when testing remote DB)
# SPRING_PROFILES_ACTIVE=prod
